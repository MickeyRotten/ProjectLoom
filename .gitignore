# dependencies
node_modules/

# build output
dist/
dist-ssr/
*.local

# vite / vitest
.vite/
coverage/

# TS incremental build info
*.tsbuildinfo

# Capacitor / Android
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/
android/app/debug/
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/
*.keystore
*.jks
keystore.properties
# ...except the shared debug keystore CI signs fallback builds with, so every
# published debug APK has a stable signature and updates install (non-secret).
!.github/ci/loom-debug.keystore

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
